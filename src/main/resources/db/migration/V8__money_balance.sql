-- V8__money_balance.sql
-- Переход от "чек = N блюд" к денежному балансу.
-- Баланс = сумма всех APPROVED чеков − (число блюд × цена этого человека).
-- Блюдо можно взять, если баланса хватает на его цену.

ALTER TABLE lunch_receipt ADD COLUMN amount INT NOT NULL DEFAULT 0;

-- Колонка spent из V6 больше не нужна: тратим не чек целиком, а деньги.
-- Оставляем её (не мешает), но логика теперь по сумме.
